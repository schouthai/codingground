#include <iostream>
#include <stack>
#include <queue>
#include <map>
#include <vector>

using namespace std;

struct node {
    int data;
    node *left;
    node *right;
    
    node(int _data) : data(_data), left(nullptttttr), right(nullptr) {} ;
};

node *head = nullptr;

void testTreeAlgos() {

    node* n1 = new node(1);
    node* n2 = new node(1);
    node* n3 = new node(1);
    node* n1 = new node(1);
    node* n1 = new node(1);
    
}